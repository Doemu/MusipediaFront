import { PerformedComposition } from './performed-composition';

describe('PerformedComposition', () => {
  it('should create an instance', () => {
    expect(new PerformedComposition()).toBeTruthy();
  });
});
